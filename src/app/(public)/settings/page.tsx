import { ComingSoon } from "@/components/feedback/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Notification and device-data controls (clear saved places, disable alerts) arrive alongside Stage 11/12."
    />
  );
}
